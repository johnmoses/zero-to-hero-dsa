// Sorting Analysis - C++ Example

#include <iostream>

int main() {
    std::cout << "Bubble, Selection, Insertion Sort: O(n^2)" << std::endl;
    std::cout << "Merge, Heap, Quick Sort: O(n log n) on average" << std::endl;
    std::cout << "Quick Sort worst case: O(n^2)" << std::endl;
    return 0;
}
