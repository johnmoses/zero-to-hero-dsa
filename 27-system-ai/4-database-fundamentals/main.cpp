// Database Fundamentals - C++ example comment
// C++ does not have built-in DB support; typically use external DB libraries
// Example provided is pseudocode illustrating SQL commands.

#include <iostream>

int main() {
    std::cout << "Use external libraries for database access in C++ like SQLiteCpp or ODBC." << std::endl;
    std::cout << "Example SQL query: SELECT * FROM users;" << std::endl;
    return 0;
}
